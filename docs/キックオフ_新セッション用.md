# DOSL HUB 再開用キックオフ（新セッションに貼るだけ）

```
DOSL HUB（展示会ドキュメント管理システム）の続きをやります。
場所: /Users/alltokyo42/Projects/exhibition-systems/document-management/
まず docs/HANDOVER.md（2026-07-12更新・6回目）の冒頭を読んで現状を把握してください。

現状の要点:
- 開発は全指示書（01〜05）完了・全機能が本番稼働中。開発側の残タスクはゼロ
- GCP本番: https://34-168-97-181.sslip.io/login.html
  （運用・再デプロイは deploy/公開手順_GCP.md。scp前の転送先削除とハッシュ照合を必ず守る）
- ログイン・メール・Resendキーはすべて「本番ログイン情報.txt」（このMacのみ・git管理外）参照
- ファイル保存はGCS（gs://dosl-hub-documents）。メール提出=hub@dosl.co.jpのIMAPポーリング・
  通知送信=Resend API（XserverはGCP-IPからのSMTP拒否のため）
- デモ環境あり: デモ展示会＋demo-organizer/demo-exhibitorアカウント（外部に見せてよい）
- マニュアル2冊は docs/マニュアル/（2026-07-12改訂済み・配布は大西さん確認待ち）

今日やりたいこと:
（例1）実展示会と実出展社を登録して実運用を始めたい（→デモアカウント無効化も忘れずに）
（例2）7/8実装分の多角的バグ探しレビューをやって
（例3）×××のバグを直して / ×××を改善して
```

## メモ（貼る前に確認）
- 検証は必ず実ブラウザで（curlで止めない）。テスト用DB（port 8713）を使いローカル開発DBを汚さない
- 本番反映は deploy/公開手順_GCP.md の「更新」手順どおり。VM上の backend/.env は上書き禁止・追記のみ
- 本番のIAM変更・VM停止起動・資格情報の取得は自動モードでブロックされることがある→ユーザーの明示承認を得て実行
- pytest: `cd backend && .venv/bin/python -m pytest -q`（13件。メール取込ロジック＋IMAP解析層）
- push先 `mitsuhiro-onishi/test0406` へのpushは外部反映のため要確認
- DOSL GATE（隣の registration-system/）とは別案件。メール送信は両方Resendだがキーは別
  （HUB用キーはResendダッシュボード「dosl-hub」）
- ResendはUser-Agentヘッダ無しを403で弾く／Xserverの本番SMTP送信は不可（クラウドIP拒否）——ハマりポイント
- 実運用開始時: デモアカウント（demo-organizer/demo-exhibitor）をユーザー管理画面から無効化
