# DOSL HUB 再開用キックオフ（新セッションに貼るだけ）

```
DOSL HUB（展示会ドキュメント管理システム）の続きをやります。
場所: /Users/alltokyo42/Projects/exhibition-systems/document-management/
まず docs/HANDOVER.md（2026-07-08更新・5回目）の冒頭
「今の反映状況と次のアクション」を読んで現状を把握してください。

現状の要点:
- GCP本番公開済み: https://34-168-97-181.sslip.io/login.html
  （運用・再デプロイは deploy/公開手順_GCP.md。scp前の転送先削除とハッシュ照合を必ず守る）
- 本番ログインは「本番ログイン情報.txt」（このMacのみ・git管理外）参照
- 指示書01/02/05は本番反映済み。03（GCS化）と04＋5-4（メール受信/送信）は
  コード完成・コミット済みだが未デプロイ（詳細と残手順はHANDOVER冒頭）
- 使い方マニュアル2冊が docs/マニュアル/ にある（再生成は tools/README.md）

今日やりたいこと:
（例1）GCS切替を実行して。ダウンタイム数分は了承済み
（例2）Gmailアカウントを用意したのでメール受信を有効化して。
       クライアントID/シークレットは○○○
（例3）×××のバグを直して / ×××を改善して
```

## メモ（貼る前に確認）
- 検証は必ず実ブラウザで（curlで止めない）。テスト用DB（port 8713・launch.jsonの dosl-hub-test）を使い
  ローカル開発DBを汚さない
- 本番反映は deploy/公開手順_GCP.md の「更新」手順どおり。VM上の backend/.env は上書き禁止・追記のみ
- 本番デプロイ・IAM変更・VM停止起動は自動モードでブロックされることがある→ユーザーの明示承認を得て実行
- pytest: `cd backend && .venv/bin/python -m pytest tests/ -v`（メール取込の判定ロジック）
- push先 `mitsuhiro-onishi/test0406` へのpushは外部反映のため要確認
- DOSL GATE（隣の registration-system/）とは別案件。メール送信はGATE=Resend・HUB=Gmailで方式が違う点に注意
