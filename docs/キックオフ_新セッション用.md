# DOSL HUB 再開用キックオフ（新セッションに貼るだけ）

```
DOSL HUB（展示会ドキュメント管理システム）の続きをやります。
場所: /Users/alltokyo42/Projects/exhibition-systems/document-management/
まず docs/HANDOVER.md（2026-07-07更新）を読んで現状を把握してください。

現状: ローカルで全機能動作済み（./run_dev.sh → http://localhost:8710/login.html、
シードは POST /api/seed、デモアカウントは backend/app/api/seed.py）。

今日やりたいこと: ①ANTHROPIC_API_KEYを設定した実AI解析テスト
（または ②GCPデプロイ準備 / ③別タスク: HANDOVER.mdの残タスク表参照）
```

※ ①をやる場合は backend/.env に `ANTHROPIC_API_KEY=sk-...` を書いてから起動する。
